{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GitHub Document Loader",
  "description": "Use GitHub data as input to this chain",
  "x-n8n-node-type": "documentGithubLoader",
  "x-n8n-version": [
    1,
    1.1
  ],
  "x-n8n-group": [
    "transform"
  ],
  "type": "object",
  "properties": {
    "repository": {
      "x-title": "Repository Link",
      "default": "",
      "type": "string"
    },
    "branch": {
      "x-title": "Branch",
      "default": "main",
      "type": "string"
    },
    "textSplittingMode": {
      "x-title": "Text Splitting",
      "default": "simple",
      "x-displayOptions": {
        "show": {
          "@version": [
            1.1
          ]
        }
      },
      "x-required": true,
      "type": "string",
      "enum": [
        "simple",
        "custom"
      ],
      "x-enumNames": [
        "Simple",
        "Custom"
      ],
      "x-enumDescriptions": [
        "Splits every 1000 characters with a 200 character overlap",
        "Connect a custom text-splitting sub-node"
      ]
    },
    "additionalOptions": {
      "x-title": "Options",
      "default": {},
      "type": "object",
      "properties": {
        "recursive": {
          "x-title": "Recursive",
          "default": false,
          "type": "boolean"
        },
        "ignorePaths": {
          "x-title": "Ignore Paths",
          "description": "Comma-separated list of paths to ignore, e.g. \"docs, src/tests",
          "default": "",
          "type": "string"
        }
      }
    }
  },
  "x-icon": "file:github.svg",
  "x-inputs": "={{ ((parameter) => { function getInputs(parameters) {\n    const inputs = [];\n    const textSplittingMode = parameters?.textSplittingMode;\n    if (!textSplittingMode || textSplittingMode === 'custom') {\n        inputs.push({\n            displayName: 'Text Splitter',\n            maxConnections: 1,\n            type: 'ai_textSplitter',\n            required: true,\n        });\n    }\n    return inputs;\n}; return getInputs(parameter) })($parameter) }}",
  "x-outputs": [
    "ai_document"
  ],
  "x-credentials": [
    {
      "name": "githubApi",
      "required": true
    }
  ]
}
